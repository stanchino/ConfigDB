.row
  = simple_form_for(@category, url: [@organization, @category], html: {class: 'col-md-6'}) do |f|
    = f.error_notification

    .form-inputs
      = f.input :name
      = f.input :textkey

    .form-actions
      = f.button :submit
